import "./Loading.scss";
import loading from "../../assets/icons/loading.gif";
const Loading = () => {
	return <img src={loading} alt="Loading..." className="loading-icon"/>;
};

export default Loading;
